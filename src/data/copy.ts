/** Visible / accessibility copy from Sauce Labs My Demo App — keep out of page methods. */
export const navigationCopy = {
  viewCart: 'View cart',
  viewMenu: 'View menu',
  loginMenuItem: 'Login Menu Item',
  catalogMenuItem: 'Catalog',
} as const;

export const loginCopy = {
  usernamesHeader: 'Usernames',
  loginButton: 'Login',
} as const;

export const catalogCopy = {
  productsHeader: 'Products',
} as const;

export const cartCopy = {
  title: 'My Cart',
  empty: 'No Items',
  removeItem: 'Remove Item',
  itemCountSuffix: 'Items',
} as const;

export const productDetailsCopy = {
  addToCartLabel: 'Tap to add product to cart',
} as const;

export const itemCountPattern = /^\d+ Items$/;
